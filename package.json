{
	"name": "root",
	"private": true,
	"scripts": {
		"clean": "npx rimraf --glob .nx/cache && npx lerna run clean",
		"validate": "npx lerna run validate",
		"test": "npx lerna run test",
		"build": "npx lerna run build",
		"package": "npx lerna run package",
		"install": "npx lerna exec -- npm install"
	},
	"dependencies": {},
	"devDependencies": {
		"lerna": "8.1.2",
		"rimraf": "^6.0.1"
	},
	"notes": [
		"clean",
		"validate",
		"transpile",
		"test-transpile",
		"test",
		"package"
	]
}
