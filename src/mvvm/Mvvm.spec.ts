import { assert, expect } from "chai";
import { describe, it } from "mocha";
import { anything, instance, mock, spy, verify, when } from "ts-mockito";

describe("Mvvm tests", () => {
	it.skip("Mvvm new instance", () => {
		assert.isTrue(false);
	});

});
