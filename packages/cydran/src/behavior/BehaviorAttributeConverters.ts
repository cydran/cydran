import SimpleMap from "interface/SimpleMap";

interface BehaviorAttributeConverters extends SimpleMap<(value: string) => unknown> {

	// Intentionally empty

}

export default BehaviorAttributeConverters;
